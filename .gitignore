# Dependencies
/node_modules

# Next.js
/.next/
/out/

# Environment variables (/env.ts guards a past incident: real secrets were
# once saved under that filename and it isn't a source file)
.env
.env.local
.env.*.local
/env.ts

# Vercel
.vercel

# TypeScript
*.tsbuildinfo
next-env.d.ts

# Supabase
/supabase/.branches
/supabase/.temp

# Claude Code: skills and shared settings are versioned; local overrides are not.
.claude/settings.local.json
.claude/worktrees/
